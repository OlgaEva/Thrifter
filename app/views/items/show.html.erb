<h1> THRIFTER </h1>

<%= link_to "Back", items_path %>

<h2><%= @item.name %></h2>

<h3><%= @item.category %></h3>
<h3><%= @item.price %></h3>

<hr>

<h3><%= @item.description %></h3>

<%= @item.img_url %>


<form method="get" action="/buyers/new">
<input type="hidden" name="id" value=<%= @item.id %>>
<input type="submit" value="buy"/>
</form>

<nav>
<%= link_to "Back to all Items", items_path %>
<br>
<%= link_to "Post a New Item for Sale", new_item_path %>
<br>
<%= link_to "Manage My Account", user_path %>
<br>
<!-- Is this the logout path? -->
<%= link_to "Logout", "/logout" %>
<br>

</nav>
<%= button_to "Delete Item", item_path(@item.id), method: :delete %>